name: Build Android Project

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Download and set up Android SDK
      uses: android-actions/setup-android@v2
      with:
        api-level: 30
        build-tools: 30.0.3

    - name: Download and set up NDK
      run: |
        echo "::group::Download NDK"
        wget https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
        unzip android-ndk-r26d-linux.zip -d $HOME
        echo "::endgroup::"
        echo "NDK_HOME=$HOME/android-ndk-r26d" >> $GITHUB_ENV

    - name: Add NDK to PATH
      run: echo "$HOME/android-ndk-r26d" >> $GITHUB_PATH

    - name: Display NDK version
      run: $HOME/android-ndk-r26d/ndk-build --version

    - name: Build with Gradle
      run: ./gradlew assembleRelease
      env:
        ANDROID_SDK_ROOT: ${{ runner.tool_cache }}/android-sdk
        NDK_HOME: ${{ env.NDK_HOME }}
